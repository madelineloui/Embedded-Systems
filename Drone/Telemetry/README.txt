# Processing Accelerometer Data into a CSV File

Takes text files with Unix timestamps, accelerometer readings, and gyroscope readings and parses the data.
Converts the Unix timestamps to Human Readable Format and calculates the angle of inclination of the drone based on the accelerometer readings.
Outputs a csv file with the time and corresponding angle in each row. 
